import 'package:flutter/material.dart';

class ErrorWidgetMine extends StatelessWidget {
  const ErrorWidgetMine({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Error"),
      ),
    );
  }
}
